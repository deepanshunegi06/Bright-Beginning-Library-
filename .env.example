# Environment Variables Template
# Copy this to .env.local and update with your actual values

# MongoDB Connection String
# Get from MongoDB Atlas: Database > Connect > Drivers
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database-name?retryWrites=true&w=majority

# Admin Credentials
# Set your own secure admin credentials
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=your_secure_password

# WiFi Network Name
# Replace with your library's WiFi SSID
LIBRARY_WIFI_SSID=Your_Library_WiFi
NEXT_PUBLIC_LIBRARY_WIFI_SSID=Your_Library_WiFi

# Library Network IP Prefix (for local development only)
# First 3 octets of your local network IP (e.g., 192.168.1)
LIBRARY_NETWORK_PREFIX=192.168.1

# Library Geolocation (REQUIRED for Vercel/Cloud deployment)
# Get coordinates from Google Maps: Right-click library location > Copy coordinates
# Replace with your actual library coordinates
LIBRARY_LATITUDE=0.000000
LIBRARY_LONGITUDE=0.000000
# Maximum distance in meters (recommended: 100-200 for indoor GPS accuracy)
LIBRARY_RADIUS_METERS=100

# Home Location (for development/testing - Optional)
HOME_LATITUDE=0.000000
HOME_LONGITUDE=0.000000
